# คู่มือพัฒนาแบบฝึกหัดลูก

โปรเจกต์นี้เป็นเว็บแบบฝึกหัดสำหรับเด็ก ป.1 ที่เปิดเล่นผ่าน GitHub Pages ได้เลย

ลิงก์หลัก:
- หน้าเลือกหมวด: https://ditthakornm-star.github.io/eng-word-game/
- คณิตศาสตร์: https://ditthakornm-star.github.io/eng-word-game/math-quiz/
- อังกฤษ ชุด 6-7 ก.ค.: https://ditthakornm-star.github.io/eng-word-game/week-july-6/
- อังกฤษ ชุด 29-30 มิ.ย.: https://ditthakornm-star.github.io/eng-word-game/week-last/

## โครงสร้างไฟล์

```text
index.html              หน้าเลือกหมวดวิชา
math-quiz/index.html    เกมแบบทดสอบคณิตศาสตร์
week-july-6/index.html  เกมคำศัพท์อังกฤษชุดล่าสุด
week-last/index.html    เกมคำศัพท์อังกฤษชุดเก่า
.nojekyll               บอก GitHub Pages ให้เสิร์ฟไฟล์ตรงๆ
```

## หลักการออกแบบหน้าเลือกหมวด

- `index.html` เป็นหน้าศูนย์รวม ไม่ควรเอาเกมใหม่ไปปนในหน้าอังกฤษโดยตรง
- ถ้ามีวิชาใหม่ เช่น ภาษาไทย ให้สร้างโฟลเดอร์ใหม่ เช่น `thai-quiz/index.html`
- เพิ่มการ์ดลิงก์ใน `index.html` ให้แยกหมวดชัดเจน
- ลิงก์ GitHub Pages จะเป็นรูปแบบ `https://ditthakornm-star.github.io/eng-word-game/ชื่อโฟลเดอร์/`

## หลักการออกแบบเกมคณิต

ไฟล์หลักคือ `ป.1/คณิต/math-quiz-app/index.html` (บน GitHub Pages ใช้ชื่อโฟลเดอร์ `math-quiz/`)

แนวทางที่ตกลงไว้:
- มีหน้าหลักให้เลือก เล่นรวมทุกหน่วย หรือเลือกทีละหน่วย 1-7
- โหมดรวม: สุ่มหน่วยละ 6 ข้อ รวม 42 ข้อ / โหมดหน่วยเดียว: 12 ข้อ
- ทุกครั้งที่เริ่มเล่น ต้องสุ่มโจทย์และช่องตอบใหม่
- โจทย์ต้องยากพอสำหรับซ้อมสอบ ไม่ใช้โจทย์ง่ายเกินไป (ยกเว้นหัวข้อ “ศูนย์กับการบวก/การลบด้วยศูนย์” ที่ตั้งใจฝึกเรื่อง 0)
- อย่าให้รูปหรือโจทย์มีเฉลยแอบอยู่ เช่น ไฮไลต์คำตอบในภาพ
- ใช้อีโมจิสิ่งของ/อาหาร/เครื่องเขียน แทนหน้าเด็กหรือสัตว์
- ไม่มีเสียงอ่านโจทย์ เพราะ browser อ่านคำไทยอย่าง “บวก/ลบ” เพี้ยน
- เหลือเฉพาะเสียงเอฟเฟกต์ตอนแตะ ตอบถูก และตอบผิด

หน่วยครบตามหนังสือเรียน (7 หน่วย):
- หน่วย 1: จำนวนนับ 1-10 และ 0
- หน่วย 2: การบวกที่ผลบวกไม่เกิน 10
- หน่วย 3: การลบที่ตัวตั้งไม่เกิน 10
- หน่วย 4: จำนวนนับ 11-20
- หน่วย 5: การบวกที่ผลบวกไม่เกิน 20 (มีบวกสามจำนวน)
- หน่วย 6: การลบที่ตัวตั้งไม่เกิน 20
- หน่วย 7: รูปเรขาคณิต 2 มิติ และ 3 มิติ (วาดด้วย SVG ในโค้ด)

รูปแบบการเล่น (input.type) ที่เพิ่มมาให้เหมาะกับจอสัมผัส — ดัดแปลงจากแบบฝึกหัดจริง:
- `pad` แผงตัวเลข 0-10 หรือ 0-20 ให้แตะตอบ (แทนช่องเขียนในสมุด)
- `compare` ปุ่ม `>` `<` `=` ขนาดใหญ่
- `operator` ปุ่ม `+` `-` (เติมเครื่องหมายให้ได้คำตอบ)
- `tenframe` แตะช่องให้ครบตามจำนวน (ระบายให้ครบ)
- `order` แตะการ์ดเรียงลำดับน้อย→มาก หรือ มาก→น้อย
- `bondmake` แตะสองจำนวนที่รวมกันได้ตามส่วนรวม (ตอบได้หลายแบบ)
- `choice` เลือกตอบ ใช้กับชื่อรูปเรขาคณิต/เลือกประโยคสัญลักษณ์

เวลาจะเพิ่มความยากหรือหัวข้อ:
- เพิ่ม `type` ใหม่ใน `genUnitX()` ของหน่วยนั้น แล้วให้คืน `input.type` ที่รองรับ
- ถ้าต้องการ input แบบใหม่ ให้เพิ่ม `renderXxx()` และผูกใน `renderInput()` โดยจบด้วยเรียก `finish(q, correct)`
- ระวังตัวเลือกหลอกในโหมด choice ต้องมีคำตอบถูกเพียงข้อเดียวและไม่ซ้ำ
- สำหรับข้อเปรียบเทียบ ให้ถามแบบเจาะจง เช่น “จำนวนใดน้อยกว่า 20 อยู่ 1” แทน “จำนวนใดน้อยกว่า 20”
- มีสคริปต์ตรวจ: extract `<script>` แล้ว `node --check` และ eval generators เพื่อยืนยันคำตอบตรงกับโจทย์

## หลักการออกแบบเกมคำศัพท์อังกฤษ

ไฟล์หลักของแต่ละชุดคือ:
- `week-july-6/index.html`
- `week-last/index.html`

แนวทางที่ตกลงไว้:
- มีหน้าเลือกชุดคำศัพท์/สัปดาห์
- มีปุ่มกลับหน้าเริ่มต้น
- โหมดง่ายมาก: ให้ดูคำเต็มใน popup ก่อน กด “พร้อม” แล้วค่อยพิมพ์
- โหมดง่าย: มีคำใบ้ กดได้ไม่เกิน 2 ครั้งต่อคำ
- โหมดยาก: ไม่ให้คำใบ้
- คำยาวควรแบ่งเป็นช่วงให้จำง่าย เช่น `fibrous root` แยกเป็น `fi-brous root` หรือแยกตามจังหวะอ่านที่เด็กจำได้
- ชุดเก่าควรยังอยู่ เพื่อให้เด็กกลับไปทวนได้

## วิธีอัปเดตขึ้น GitHub Pages

แก้ไฟล์ใน repo นี้ก่อน:

```text
C:\Users\DENG\Documents\Codex\2026-06-28\gv\work\eng-word-game-repo
```

จากนั้นเช็กและอัปขึ้น GitHub:

```powershell
git status --short
git add .
git commit -m "คำอธิบายสั้นๆ ว่าแก้อะไร"
git push origin main
```

หลัง push แล้ว GitHub Pages มักใช้เวลาประมาณ 30-90 วินาทีจึงจะเห็นผล

ถ้ามือถือยังเห็นหน้าเก่า:
- ปิดแท็บแล้วเปิดใหม่
- หรือเติมท้ายลิงก์ เช่น `?v=ใหม่`
- หรือ clear cache ใน browser

## วิธีเพิ่มบทใหม่หรือวิชาใหม่

1. สร้างโฟลเดอร์ใหม่ เช่น `thai-quiz/` หรือ `math-unit-5/`
2. ใส่ไฟล์ `index.html` ในโฟลเดอร์นั้น
3. เพิ่มปุ่มลิงก์ในหน้า `index.html` หลัก
4. ทดสอบในเครื่องก่อน
5. commit และ push
6. เปิดลิงก์ GitHub Pages เพื่อตรวจจริง

## สิ่งที่ต้องระวัง

- อย่าใช้ชื่อไฟล์เกมหลักเป็นชื่ออื่นถ้าจะให้ GitHub Pages เปิดได้ง่าย ต้องใช้ `index.html`
- อย่าเอาเกมหลายวิชาไปปนในหน้าเดียวกัน ให้แยกหมวดชัดๆ
- อย่าใช้ Text-to-Speech ภาษาไทยในโจทย์คณิตตอนนี้ เพราะเสียงอ่านเพี้ยน
- เวลาทำโจทย์สุ่ม ต้องเช็กว่าไม่มีคำตอบซ้ำหรือคำถามกำกวม
- ถ้าแก้จาก GitHub หน้าเว็บโดยตรง ต้องระวังอย่าลบโครงสร้างไฟล์เดิม
